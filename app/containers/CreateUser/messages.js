/*
 * CreateUser Messages
 *
 * This contains all the text for the CreateUser container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateUser container!',
  },
});
