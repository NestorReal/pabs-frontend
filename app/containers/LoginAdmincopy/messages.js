/*
 * LoginAdmin Messages
 *
 * This contains all the text for the LoginAdmin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoginAdmin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoginAdmin container!',
  },
});
