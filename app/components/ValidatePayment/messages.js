/*
 * ValidatePayment Messages
 *
 * This contains all the text for the ValidatePayment component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ValidatePayment';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ValidatePayment component!',
  },
});
